import React from 'react';

const RoutLineSwitch = () => (
  <div className="route-line-switch">
    <span className="route-line-switch-name">Interchange for yellow line</span>
  </div>
);

export default RoutLineSwitch;
