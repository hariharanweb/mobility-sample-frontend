import React from 'react';
import RouteLine2 from './RouteLine2';

const RouteDetails2 = ({ routeDetails }) => (
  <div className="route-details-section">
    <RouteLine2 steps={routeDetails} />

  </div>
);

export default RouteDetails2;
